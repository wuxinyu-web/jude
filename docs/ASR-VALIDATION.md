# 原声转写验证 · 1.6.0

日期：2026-09-12；本机 Apple Silicon Mac。

## 真实视频验证（独立于自动化测试）

- 视频：BV1bfLwz1Eu4，合伙人之路第一季第 01 集，原站字幕为 ai-zh。
- Chrome 现有开发扩展已重新加载至 1.6.0，扩展 ID 保持不变。
- 用户在当前任务中要求实际英文音频转写，测试通过该扩展「转写英文原声」按钮启动。
- yt-dlp 直接读取公开视频原声，不导入浏览器 Cookie；本地 ffmpeg 解码并以 Whisper small.en 执行英文 transcribe。
- 完整音频 2819.83 秒，生成 873 条带时间戳的识别片段；服务从任务创建到完成用时约 77.1 秒。
- 完成后侧栏自动显示英文原声识别标记及英文正文。实际点击双语按钮后显示英文与原站中文按时间对照。
- 模型是自动语音识别，未逐句人工听校。抽查可见人名、快速对白有误识别；本次验证证明真实音频处理与接入成功，不代表逐字准确率。
- 临时下载音频已由 worker 清理；任务 JSON 保留在本机，用于恢复。

## 自动化与回归

- npm test：191 项通过。
- npm run check：通过，39 个运行资源白名单。
- npm run package：通过，生成 1.6.0 ZIP。
- Python 本地服务：6 项通过，涵盖来源、Host、CORS、非法参数、任务恢复、取消进程和临时文件清理。
- 独立 Chrome 配置下 ASR 接口模拟集成：通过中文不重复翻译、取消、英文应用、原字幕备份、时间对齐、恢复和错误视频拒绝。测试不调用真实 AI 提供商。
- 独立 Chrome 配置下 B 站完整回归：收藏、学习计时、跟随、复习、Word 下载等既有流程通过。
- 依赖 pip check 通过；模型固定提交及 SHA-256 校验通过。
- 本次未重新进行 Word 逐页人工打印检查，沿用既有导出回归；本次功能不改变 Word 排版。

## 已知限制

本地服务当前面向 Apple Silicon Mac，单次最多 90 分钟、150 MB，支持可公开读取原声的 BV 视频及分 P。受限视频无法读取时明确失败。分 P ID 保留有单测覆盖，本次真实视频验证使用第 1 P。

原站 ai-zh 本身可能有错字；按时间对照也可能在相邻英文段落重复显示跨界的中文片段。无法把这一对照视作人工校订的逐句译文。

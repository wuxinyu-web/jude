const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs');
test('immersion reuses transcript controls and supports direct sidebar return',()=>{const source=fs.readFileSync('lib/immersive.js','utf8');assert.match(source,/setTranscriptMode\('bilingual'\)/);assert.match(source,/mode:'horizontal'/);assert.doesNotMatch(source,/immersiveCue|translationQueue|setInterval/);});
test('main workspace has one immersive arrow, and settings no longer offer a separate vertical workspace',()=>{const panel=fs.readFileSync('sidepanel.html','utf8'),settings=fs.readFileSync('options.html','utf8');assert.match(panel,/id="enterImmersive"/);assert.doesNotMatch(panel,/id="moveLearningPanel"/);assert.doesNotMatch(settings,/value="vertical"/);});
